from setuptools import setup, Extension

module = Extension('mykmeanssp', sources=['kmeansmodule.c'])

setup(name='mykmeanssp', version='1.0', description='K-means implementation in C with Python extension', ext_modules=[module])
